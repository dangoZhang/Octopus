{
  "$schema": "../tentacle.schema.json",
  "schema_version": "0.0.1",
  "id": "swe-agent",
  "name": "SWE Agent Tentacle",
  "description": "LLM-guided repository work with editable tool metadata and code implementations.",
  "brain": {
    "kind": "llm",
    "model": null,
    "prompt": "Map a cognitive Need to the smallest repo action, select tools from metadata, execute implementation code, and return compact patch/test evidence.",
    "feedback_contract": "Return status, evidence, touched files, and the next useful Need. Do not return hidden reasoning."
  },
  "skills": [
    {
      "id": "swe-workflow",
      "description": "Read, edit, inspect, patch, and verify a repository.",
      "needs": ["observe", "execute", "verify", "reproduce"]
    }
  ],
  "tools": [
    {
      "id": "read",
      "description": "Read a safe line range from a workspace file.",
      "input": "file path plus optional start/end lines",
      "output": "plain text file slice",
      "implementation": { "kind": "shell", "entrypoint": "tools/read.sh" }
    },
    {
      "id": "edit",
      "description": "Apply one scoped text replacement.",
      "input": "file path, old text, new text",
      "output": "edit status",
      "implementation": { "kind": "shell", "entrypoint": "tools/edit.sh" }
    },
    {
      "id": "inspect_repo",
      "description": "Summarize repo state and project type.",
      "input": "workspace root",
      "output": "status, files, and detected manifests",
      "implementation": { "kind": "shell", "entrypoint": "tools/inspect_repo.sh" }
    },
    {
      "id": "write_patch",
      "description": "Write a patch file and optionally apply it.",
      "input": "patch on stdin",
      "output": "patch path and apply-check status",
      "implementation": { "kind": "shell", "entrypoint": "tools/write_patch.sh" }
    },
    {
      "id": "run_tests",
      "description": "Run detected project tests.",
      "input": "workspace root",
      "output": "test output",
      "implementation": { "kind": "shell", "entrypoint": "tools/run_tests.sh" }
    }
  ],
  "evolution": {
    "editable": ["manifest.json", "brain.prompt", "tools/*", "adapters/*"],
    "surfaces": [
      {
        "id": "brain_prompt",
        "description": "Tool-side planning prompt and feedback contract.",
        "targets": ["brain.prompt", "brain.feedback_contract"]
      },
      {
        "id": "tool_meta",
        "description": "Repo tool descriptions, inputs, outputs, and call contracts.",
        "targets": ["tools[].description", "tools[].input", "tools[].output", "tools[].implementation.contract"]
      },
      {
        "id": "runtime_code",
        "description": "Editable repository harness implementations across runtimes.",
        "targets": ["tools/*", "tools[].implementation.kind", "tools[].implementation.entrypoint"]
      },
      {
        "id": "evolution_policy",
        "description": "Checks and constraints that keep repo edits safe.",
        "targets": ["evolution.checks", "evolution.constraints", "evolution.surfaces"]
      }
    ],
    "checks": ["tools/read.sh README.md 1 2", "cargo test"],
    "constraints": ["Keep path safety checks unless a stronger sandbox replaces them."]
  }
}
